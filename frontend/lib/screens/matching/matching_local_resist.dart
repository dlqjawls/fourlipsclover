import 'package:flutter/material.dart';

class MatchingLocalResistScreen extends StatelessWidget {
  const MatchingLocalResistScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('현지인 계획서 작성 화면', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
